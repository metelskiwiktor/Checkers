package pl.wiktor.backend.player;

import pl.wiktor.backend.mechanism.Mechanism;

public class White extends Player{
    public White(Mechanism mechanism) {
        super(PlayerType.WHITE, mechanism);
    }
}
